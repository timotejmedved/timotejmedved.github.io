import { projects } from '../../../data/projects.js';

export default function handler(req, res) {
/*
    console.log("Z PARAMETROM .............." + req.query.id);
    const project = projects.find(project => project.id === parseInt(req.query.id));
    console.log(project);
    res.status(200).json(project);
*/

}
